// MapPage.jsx
// Temporary stub for the Map page. Will display an OpenStreetMap view of Northern Ghana
// with color-coded markers for each sanitation facility based on condition status.
// Route: /map (protected — requires login)

import React from "react";

function MapPage() {
  return (
    <div style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>Facility Map</h1>
      <p>OpenStreetMap with facility markers coming in a later session.</p>
    </div>
  );
}

export default MapPage;
