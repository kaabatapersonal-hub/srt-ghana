// ReportPage.jsx
// Temporary stub for the Report Submission page. Will contain the form for field agents
// to submit sanitation facility condition reports (with photo upload and GPS location).
// Route: /report (protected — requires login)

import React from "react";

function ReportPage() {
  return (
    <div style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>Submit Report</h1>
      <p>Report submission form coming in a later session.</p>
    </div>
  );
}

export default ReportPage;
