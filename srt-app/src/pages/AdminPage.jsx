// AdminPage.jsx
// Temporary stub for the Admin panel. Will allow admins to approve/reject submitted reports,
// manage facilities, and view system-wide analytics.
// Route: /admin (protected — requires admin role)

import React from "react";

function AdminPage() {
  return (
    <div style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>Admin Panel</h1>
      <p>Admin controls coming in a later session.</p>
    </div>
  );
}

export default AdminPage;
